# =============================================================================
# CryptoTrader AI — Python Dependencies
# =============================================================================

# ── Core ──────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
aiohttp==3.10.10
httpx==0.27.2
python-dotenv==1.0.1
structlog==24.4.0
apscheduler==3.10.4

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
aiosqlite==0.20.0
alembic==1.13.3

# ── LLM Providers ─────────────────────────────────────────────────────────────
# anthropic is installed by default (LLM_PROVIDER=anthropic is the default)
# install the SDK for whichever provider you select in LLM_PROVIDER

anthropic>=0.40.0            # LLM_PROVIDER=anthropic  (default)
# openai>=1.51.0             # LLM_PROVIDER=openai      → pip install openai
# groq>=0.11.0               # LLM_PROVIDER=groq        → pip install groq
# google-genai>=0.3.0        # LLM_PROVIDER=gemini (alt)→ pip install google-genai
# mistralai>=1.0.0           # LLM_PROVIDER=mistral (alt)→ pip install mistralai
# Ollama / OpenRouter / Together / Gemini / Mistral use aiohttp directly — no extra SDK

# ── Optional: multi-exchange ──────────────────────────────────────────────────
# ccxt>=4.4.0                # 100+ exchanges → pip install ccxt
# asyncpg>=0.29.0            # PostgreSQL     → pip install asyncpg

# ── Optional: notifications ───────────────────────────────────────────────────
# python-telegram-bot>=21.6  # Telegram alerts → pip install python-telegram-bot

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
